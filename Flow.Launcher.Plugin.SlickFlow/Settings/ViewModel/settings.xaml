<UserControl x:Class="Flow.Launcher.Plugin.SlickFlow.Settings.ViewModel.SettingsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 d:DesignHeight="150" d:DesignWidth="400">
	<Grid Margin="8">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Text="DB Directory:" VerticalAlignment="Center" />
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
			<TextBox Width="300"
         Text="{Binding DbFilePath}" />
			<Button Content="Browse" Margin="6,0,0,0" Command="{Binding BrowseCommand}" />
		</StackPanel>

		<Button Grid.Row="2" Content="Save" HorizontalAlignment="Left" Width="80" Margin="0,12,0,0" Command="{Binding SaveCommand}" />
	</Grid>
</UserControl>
